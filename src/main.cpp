#include <windows.h>
#include <iostream>
#include <chrono>
#include <thread>
using namespace std;
int main()
{

	cout << "Reverse Nord VPN Tool (test) \n";
	std::this_thread::sleep_for(std::chrono::milliseconds(3000));
	cout << "Login not found, Redirecting you\n";
	std::this_thread::sleep_for(std::chrono::milliseconds(3000));
	cout << "WIP\n";
	std::this_thread::sleep_for(std::chrono::milliseconds(500000000));
	return 0;
	exit(0);


}
